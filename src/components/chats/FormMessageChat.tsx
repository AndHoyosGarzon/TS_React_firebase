import { useMessageActions } from "@/hooks/use-message-actions";

const FormMessageChat = () => {
    //hook
    //const {sendMessage} = useMessageActions()

    return <div>FormMessageChat</div>;
};

export default FormMessageChat;
